import { Container } from '@mui/material'

const sectionStyles = {
  height: '100vh',
  display: 'flex',
}

function Buylist() {
  return (
    <Container
      id="whatsapp-buylist"
      sx={sectionStyles}
    >
    </Container>
  )
}

export default Buylist
